# App host and port configuration
APP_HOST=0.0.0.0
APP_PORT=8000

# Database credentials and location
# Set DB_HOST=db when running with Docker Compose.
# Set DB_HOST=localhost when running the app locally outside of Docker.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=library
